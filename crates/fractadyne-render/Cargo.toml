[package]
name = "fractadyne-render"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Render orchestration: scheduler, tiling, tile cache (planned — DESIGN.md §11)."
# Roadmap placeholder (empty). The render orchestration currently lives in
# fractadyne-app/src/{render,scripting}.rs; this crate is the intended future home once that
# code has a headless, method-based API (see README.md). Not published while empty.
publish = false

[dependencies]
